#!/bin/bash
# bashnative/unlink - call fs rm on a single file
BASHNATIVE="${BASHNATIVE:-$(dirname "$0")/..}"
. "${BASHNATIVE}/function/fs" || exit 1

if [[ -z "$1" ]] || [[ "$1" == "--help" ]]; then
   echo "Usage: unlink FILE"
   [[ -z "$1" ]] && exit 1 || exit 0
fi
fs rm "$1"
